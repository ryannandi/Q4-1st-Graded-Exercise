<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="author" content="Miggy Alvarado">
  <meta name="description" content="This web page outputs the happy birthday song sung by n people">
  <meta name="keywords" content="birthday, happy, lyrics, people">
  <title>Happy Birthday</title>

  <!-- Load W3.CSS framework for responsive styling -->
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

  <!-- Link to custom local CSS for additional styling -->
  <link href="style.css" rel="stylesheet" type="text/css" />

  <!-- Favicon for browser tab -->
  <link rel="icon" type="image/x-icon" href="images/favicon.ico">
</head>

<!-- Page content container using W3.CSS utility classes -->
<body class="w3-container w3-auto">

  <!-- Navigation Bar -->
  <div class="w3-bar w3-black">
    <a href="/" class="w3-bar-item w3-button w3-mobile">Home</a>
    <a href="#" class="w3-bar-item w3-button w3-mobile">Happy</a> 
  </div>

  <!-- Header Section -->
  <header class="w3-container" id="header"> 
    <h1>Ryann Andrea Lakip - 9-Mg - May 2025</h1>
    <h2>Birthday</h2>
  </header>

  <!-- Output Section -->
  <section class="w3-container" id="input"> 
    <h2>Output</h2>
    
    <!-- Display all user-submitted form data using Handlebars each-loop -->
    <div class="w3-card w3-padding w3-margin">
      {{#each formData}}
        <p>{{@key}}: {{this}}</p>
      {{/each}}
    </div>
    
    <!-- Generate the Happy Birthday lyrics with singers' names -->
    <h2>Happy Birthday</h2>
    <div class="w3-card w3-padding w3-margin">
      <p>Nickname: {{name}}</p>
      <p>Gender: {{gender}}</p>
      <p>Number of guests: {{number}}</p>

      <!-- Loop through the generated song lines -->
      {{#each song}}
        <p>{{singer}}: {{word}}</p>
      {{/each}}
    </div>
  </section>

  <!-- Footer Section -->
  <footer class="w3-container">
    <p>&copy; 2025 Andi Lakip</p>
  </footer>

  <!-- Link to server-side script (Note: this won't run on the client-side, likely included accidentally) -->
  <script src="server.js"></script>
</body>
</html>
